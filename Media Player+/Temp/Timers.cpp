#include <Main.h>

/*
void InitializeTimers() 
{
    // Fetch Audio Outputs timer
    SetTimer(NULL, TIMER_REFRESH_AUDIO, 5000, AudioProc);
	OutputDebugString(L"Set timer started\n");
}


// Timer procedure

void CALLBACK AudioProc(HWND hwnd, UINT uMsg, UINT_PTR idEvent, DWORD dwTime)
{
	OutputDebugString(L"Timer proc started\n");
	switch (idEvent) {
	case TIMER_REFRESH_AUDIO:
		FetchAudioOutputs();
		break;
	default:
		break;
	}
}
*/